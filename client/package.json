{
  "name": "@zipper/client",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace=@zipper/webview run dev",
    "build": "npm --workspace=@zipper/webview run build",
    "start": "npm --workspace=@zipper/webview run start"
  }
}
